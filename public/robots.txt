# Allow all bots to access everything
User-agent: *
Disallow:

# Allow Googlebot full access (optional, redundant since it's already allowed by default)
User-agent: Googlebot
Disallow:

# Add the location of your sitemap
Sitemap: https://pasar.web.id/api/sitemap
